---
//          [IMPORT]
import LoginForm from "@c/Library/Form/LoginForm.jsx";
//          [DATA]
const { data, className } = Astro.props;
---
{(className == 'login') && <LoginForm client:only="react" data={data} ><slot /></LoginForm>}
