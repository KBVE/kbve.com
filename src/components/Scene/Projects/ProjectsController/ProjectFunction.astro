---
const { id } = Astro.props;
const data = await fetch('https://kbve.com/project/project.json').then((response) =>  response.json());
if(id != '*')
{

}
---
{data.map(project => (

    <article class="flex flex-col">
        <a rel="noopener noreferrer" href={project.slug} aria-label="Te nulla oportere reprimique his dolorum">
            <img alt="" class="object-cover w-full h-52 rounded-t-full" src={project.img} loading="lazy">
        </a>
        <div class="flex flex-col flex-1 p-6 bg-offset">
            <a rel="noopener noreferrer" href={project.slug} aria-label="Te nulla oportere reprimique his dolorum"></a>
            <a rel="noopener noreferrer" href={project.slug} class="text-xs tracking-wider uppercase hover:underline dark:text-orange-400">{project.title}</a>
            <h3 class="flex-1 py-2 font-semibold leading-snug">{project.description}</h3>
            <div class="flex flex-wrap p-1 justify-center text-xs ">
                {project.tag.slice(0, 3).map((tags) =>	<a rel="noopener noreferrer" href={`https://kbve.com/t/${tags}`} class="inline-block text-2xl font-semibold sm:text-3xl"><span class="px-2 py-1 text-xs rounded-full bg-orange-500">#{tags}</span></a>)}
                
            </div>
        </div>
    </article>
  ))}





  