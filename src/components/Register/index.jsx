import { h } from 'preact';
const Register = ({url = "https://api.kbve.com/api/auth/local/register", display = true}) => {
    if(display)
	return (
        <div>
            
        </div>

        );
    else
        return (<div>ඞ</div>);

}

export default Register;