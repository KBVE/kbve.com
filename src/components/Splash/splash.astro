---
import { Picture } from '@astrojs/image/components';

import Starfield from "@c/Element/Background/Starfield/starfield.astro";

import KBVELogo from "@c/Branding/KBVELogo";

import spellbook from '../../assets/spellbook.png';

// const widths = [512, 512];
// const sizes = "(min-width: 512px) 42vw, 67vw";

// const { webp, avif, png } = generateImage("src/assets/spellbook.png", {
//   widths,
//   formats: ["webp", "avif", "png"],
//   outputDir: "public/assets/images/spellbook",
//   urlPath: "/assets/images/spellbook",
// });

// const avifSrcset = avif.map(({ srcset }) => srcset).join(",");
// const webpSrcset = webp.map(({ srcset }) => srcset).join(",");
// const pngSrcset = png.map(({ srcset }) => srcset).join(",");
---
<section class="relative h-full  bg-black">
  <Starfield /> <!-- <Galaxia /> -->  <!--<div id="splash-bg-fallback" class="absolute inset-0 hidden opacity-40">  <HeroImage />  </div>  -->
  <div class="relative grid h-full sm:grid-cols-2 place-items-center">
      <h2 class="flex flex-col self-end gap-2 sm:gap-4 sm:self-auto sm:justify-self-end">    
        <div class="py-6 space-y-8"></div>
        <center><KBVELogo /></center>
        <div class="font-extrabold tracking-tighter text-center text-8xl gradient-text">
          Building
          <br /> Apps,
          <br /> Games & Memes.
        </div>
          <div class="font-extrabold tracking-tighter text-center text-2xl gradient-text">
            Some machine learning too, 
            <br /> maybe printer go brrrrr.
          </div>
      </h2>
      <Picture  class="self-start w-2/3 max-w-3xl sm:w-10/12 sm:self-auto sm:justify-self-start animate-pulse" src={spellbook} widths={[200, 400, 800]} aspectRatio="1:1" sizes="(max-width: 800px) 100vw, 800px" formats={['avif', 'png', 'webp']} alt="The KBVE Spellbook logo" />
      
  </div>
</section>
<div class="h-12 bg-gradient-to-b from-black "></div>


<noscript>
  <style>
    #splash-bg-fallback {
      display: block;
    }
  </style>
</noscript>

<style>
  @keyframes float {
    0% {
      transform: translate3d(0, 0, 0);
    }

    100% {
      transform: translate3d(0, 30px, 0);
    }
  }

  picture {
    animation: float linear 2.5s infinite alternate;
  }

  @media (prefers-reduced-motion: reduce) {
    picture {
      @apply animate-none;
    }

    :global(#starfield) {
      @apply hidden;
    }

    #splash-bg-fallback {
      @apply block;
    }
  }
</style>
