---
import Lottie from "@c/Element/Lottie";
const { content, json, width, height, id="lottie", ref = null} = Astro.props;
---
<Lottie ref={ref} id={id} json={json} width={width} height={height} client:only="react" />