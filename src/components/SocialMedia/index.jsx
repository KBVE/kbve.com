import { h } from 'preact';

// Social Media - Component that will return all social media

const SocialMedia = ({title = null, description = null, image = null, url = null, twitter}) => {
	return (<>
           
        </>);
}

export default SocialMedia;
