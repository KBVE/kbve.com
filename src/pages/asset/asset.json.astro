---
const allAsset = (await Astro.glob('./**/**/**/*.mdx'))
const json = JSON.stringify(
allAsset.map((p) => {
    return {
      title: p.frontmatter.title,
      description: p.frontmatter.description,
      slug: 'https://kbve.com' + p.url,
      tag: p.frontmatter.tags
    };
  })
);
---
{json}